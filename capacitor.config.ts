import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Formulario_de_Registros_usuarios',
  webDir: 'www'
};

export default config;
